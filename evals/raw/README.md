# Raw outputs

Keep full raw proposer/critic/judge transcripts here locally if needed. This directory is gitignored by default because raw logs can be large, noisy, and may contain sensitive content. Commit curated cases, scorecards, syntheses, and regressions instead.
